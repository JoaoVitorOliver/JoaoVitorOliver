# Em produção o site é servido pela própria API, então as chamadas são relativas
# à mesma origem. Deixe vazio — preencher aqui só é necessário se um dia o front
# for hospedado separado do back.
VITE_API_URL=
